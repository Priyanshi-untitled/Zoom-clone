{
  "name": "meetweb-zoom-clone",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "node backend/src/app.js",
    "dev": "npm run build && npm start"
  },
  "dependencies": {
    "crypto": "^1.0.1"
  }
}
